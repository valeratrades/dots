

# Here I will source f"c{number}" for i in range(10)
# Their values are gonna be stored right next to here, in json.
# API:
# c{number}
# this does `cs` into the linked directory.
# c{number} {path}
# this rewrites the value of c{number}
